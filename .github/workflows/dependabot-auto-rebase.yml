name: Dependabot auto-rebase

# rebase-strategy: auto only fires on conflicts, not when a PR is merely behind.
# With strict required status checks, behind PRs can never merge, so nudge them
# whenever main advances. Commenting is deliberate: a GITHUB_TOKEN update-branch
# push would not re-trigger CI, but a Dependabot force-push does.
on:
  push:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: dependabot-auto-rebase
  cancel-in-progress: true

jobs:
  rebase:
    runs-on: ubuntu-latest
    steps:
      - name: Require DEPENDABOT_REBASE_TOKEN
        if: ${{ secrets.DEPENDABOT_REBASE_TOKEN == '' }}
        run: |
          echo "::error::DEPENDABOT_REBASE_TOKEN is unset; github-actions[bot] comments are rejected by Dependabot"
          exit 1
      - name: Request a rebase of stale Dependabot PRs
        env:
          # github-actions[bot] comments are rejected by Dependabot. A PAT
          # (contents + pull-requests write) stored as DEPENDABOT_REBASE_TOKEN
          # is required for the comment to trigger a rebase.
          GH_TOKEN: ${{ secrets.DEPENDABOT_REBASE_TOKEN }}
          GH_REPO: ${{ github.repository }}
        run: |
          gh pr list \
            --state open \
            --base main \
            --author 'app/dependabot' \
            --json number,mergeStateStatus \
            --jq '.[] | select(.mergeStateStatus == "BEHIND" or .mergeStateStatus == "DIRTY" or .mergeStateStatus == "UNKNOWN") | .number' \
          > stale.txt
          if [ ! -s stale.txt ]; then
            echo "No stale Dependabot PRs"
            exit 0
          fi
          while read -r pr; do
            echo "Requesting rebase of #${pr}"
            gh pr comment "$pr" --body "@dependabot rebase"
          done < stale.txt
